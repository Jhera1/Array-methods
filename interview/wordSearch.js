// Given a 2D board of characters and a word, check if the word exists in the grid by connecting adjacent letters.
// Extension : Extend the function to return all valid words from a given dictionary that can be formed in the board.

const grid = [
    ['A', 'B', 'C', 'E'],
    ['S', 'F', 'C', 'S'],
    ['A', 'D', 'E', 'E']
  ]

const wordSearch = word => {
   return word
}
console.log(wordSearch('ABCCED'))